

plugins {
  id 'java'
  id 'com.parmet.buf'
}

repositories {
  mavenCentral()
}

buf {
  configFileLocation = project.file('subdir/buf.yaml')
}

dependencies {
  buf(fileTree('subdir') { include '*.yaml' })
}
